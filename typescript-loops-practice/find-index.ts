/* exported findIndex */

function findIndex(array: number[], value: number): number {
  const negativeVar: number = -1;
  for (const num of array) {
    if (value === num) {
      return num;
    } else {
    }
    return negativeVar;
  }
}
