# react-multiple-components-quiz-notes

## Quiz Questions

Answer the following questions in the provided markdown file before turning in this exercise:

- What are the phases in the React design process?

layout,

- How does data flow in React?

from parent to child, passing the data via props

- How can children components modify state in their parents?

children can't directly modify state, they can notify the parent who can then modify the state

## Notes

Pseudo code

1. layout (CSS layout and how you want the static version to look)

2. state (state setter and a variable that sets the useState)

3. events (requires to make a handler function with what you want to happen when the event is trigger)

All student notes should be written here.

How to write `Code Examples` in markdown

for JS:

```js
const data = 'Howdy';
```

for HTML:

```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:

```css
div {
  width: 100%;
}
```
