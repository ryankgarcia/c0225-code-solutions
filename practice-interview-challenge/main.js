"use strict";
function getRangeReport(start, end) {
    const report = {
        total: 0,
        odds: [],
        evens: [],
        range: [],
        average: 0,
    };
    const range = 0;
    for (let i = start; i < end; i++) {
        range.push(start + end);
    }
    return i;
}
